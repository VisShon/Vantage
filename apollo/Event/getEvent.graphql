query Query($where: EventWhere) {
	events(where: $where) {
		title
		toDate
		timestamp
		address
		sponsors {
			URL
			image
		}
		organisers {
			username
			id
		}
		fromDate
		details
		attendees {
			id
		}
		links
	}
}
